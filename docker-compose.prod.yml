services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ppp-nest-api
    container_name: ppp_nest_api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
